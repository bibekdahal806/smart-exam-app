import 'package:flutter/material.dart';

class SubjectListViewError extends StatelessWidget {
  const SubjectListViewError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
