import 'package:flutter/material.dart';

class SubjectListViewSuccess extends StatelessWidget {
  const SubjectListViewSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
